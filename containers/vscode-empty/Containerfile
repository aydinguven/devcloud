# VS Code (Empty Project)
FROM docker.io/codercom/code-server:latest

USER root

# Install essential CLI tools
RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    curl \
    wget \
    build-essential \
    unzip \
    htop \
    tree \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

USER coder
WORKDIR /home/coder/project

# Cline is available in every managed VS Code workspace. Gateway credentials
# are injected centrally when the workspace container starts.
RUN code-server --install-extension saoudrizwan.claude-dev

EXPOSE 8080

ENTRYPOINT ["/usr/bin/entrypoint.sh", "--bind-addr", "0.0.0.0:8080", "--auth", "none", "/home/coder/project"]

